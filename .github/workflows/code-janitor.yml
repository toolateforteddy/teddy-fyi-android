name: Code Janitor (Android Kotlin)

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  sweep:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Target Repo
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run Code Janitor
        uses: toolateforteddy/code-janitor@main
        with:
          provider: 'google'
          model: 'gemini-3.6-flash'
          java_version: '17'
          lint_command: './gradlew lint'
          test_command: './gradlew testDebugUnitTest'
          test_timeout: 10
          exclude_paths: '.github/workflows/**, build/**, **/build/**'
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}